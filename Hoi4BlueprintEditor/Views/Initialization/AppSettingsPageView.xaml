<Page
    x:Class="Hoi4BlueprintEditor.Views.Initialization.AppSettingsPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Hoi4BlueprintEditor.Views.Initialization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewsModels.Initialization"
    Title="AppSettingsPageView"
    d:DataContext="{d:DesignInstance vms:AppSettingsPageViewModel}"
    d:DesignHeight="{StaticResource MainWindowHeight}"
    d:DesignWidth="{StaticResource MainWindowWidth}"
    mc:Ignorable="d">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.RowSpan="4"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Image Source="{StaticResource AppSettingsMainIcon}" Style="{StaticResource SettingsIconImageStyle}" />
        </StackPanel>

        <StackPanel
            Grid.Row="0"
            Grid.RowSpan="3"
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Margin="20">
            <StackPanel Margin="0,15">
                <TextBlock Style="{StaticResource SettingsTitleTextBlockStyle}" Text="语言(Language)" />
                <ComboBox
                    ItemsSource="{Binding Languages, Mode=OneTime}"
                    SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                    Style="{StaticResource SettingsPickComboboxStyle}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName, Mode=OneTime}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <StackPanel Margin="0,15">
                <TextBlock Style="{StaticResource SettingsTitleTextBlockStyle}" Text="游戏语言(Game Language)" />
                <ComboBox
                    ItemsSource="{Binding GameLanguages, Mode=OneTime}"
                    SelectedIndex="{Binding SelectedGameLanguageIndex, Mode=TwoWay}"
                    Style="{StaticResource SettingsPickComboboxStyle}" />
            </StackPanel>
        </StackPanel>

        <StackPanel
            Grid.Row="3"
            Grid.Column="1"
            Margin="0,30"
            HorizontalAlignment="Center">
            <Button
                HorizontalAlignment="Right"
                Click="Previous_OnClick"
                Content="上一步"
                Style="{StaticResource SettingsStepButtonStyle}" />
        </StackPanel>
        <StackPanel
            Grid.Row="3"
            Grid.Column="2"
            Margin="0,30"
            HorizontalAlignment="Center">
            <Button
                HorizontalAlignment="Right"
                Click="Next_OnClick"
                Content="下一步"
                Style="{StaticResource SettingsStepButtonStyle}" />
        </StackPanel>
    </Grid>
</Page>
