<UserControl
    x:Class="Hoi4BlueprintEditor.Views.Dialogs.CreateNewFocusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewsModels.Dialogs"
    d:DataContext="{d:DesignInstance vms:CreateNewFocusViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox
            Grid.Row="0"
            Margin="4"
            VerticalAlignment="Center"
            ui:ControlHelper.Header="国策 ID"
            Text="{Binding FocusId, Mode=TwoWay}" />

        <ComboBox
            Grid.Row="1"
            Margin="4"
            ui:ControlHelper.Header="国策类型"
            ItemsSource="{Binding FocusTypes, Mode=OneTime}"
            SelectedIndex="{Binding SelectedFocusTypeIndex, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=DisplayName, Mode=OneTime}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <ComboBox
            Grid.Row="2"
            Margin="4"
            ui:ControlHelper.Header="目标文件"
            ItemsSource="{Binding FocusFileNames, Mode=OneTime}"
            SelectedIndex="{Binding SelectedFocusFileNameIndex, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=FileName, Mode=OneTime}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
