<UserControl
    HorizontalAlignment="Right"
    VerticalAlignment="Center"
    Visibility="Collapsed"
    d:DataContext="{d:DesignInstance viewsModels:FocusInfoViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="Hoi4BlueprintEditor.Views.FocusInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewsModels="clr-namespace:Hoi4BlueprintEditor.ViewsModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border
        Background="AliceBlue"
        BorderBrush="Gray"
        BorderThickness="1"
        CornerRadius="4"
        MinWidth="200"
        Padding="10">
        <StackPanel>
            <StackPanel
                AllowDrop="True"
                Drop="FocusIcon_OnDrop"
                Margin="0,0,0,8">
                <TextBlock Text="Icon" />
                <Image
                    Margin="0,0,0,8"
                    ToolTip="{Binding IconPath}"
                    x:Name="FocusIcon" />
                <TextBox Text="{Binding FocusNode.Icon, Mode=TwoWay}" />
            </StackPanel>

            <TextBox Text="{Binding FocusNode.Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ui:ControlHelper.Header="ID" />

            <StackPanel Orientation="Horizontal">
                <ui:NumberBox
                    Header="Cost"
                    Minimum="0"
                    SpinButtonPlacementMode="Inline"
                    ValidationMode="InvalidInputOverwritten"
                    Value="{Binding Cost, Mode=TwoWay}" />
                <TextBlock
                    FontSize="20"
                    Text="{Binding FocusDaysTip}"
                    VerticalAlignment="Bottom" />
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>
