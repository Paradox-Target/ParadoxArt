<UserControl x:Class="Hoi4BlueprintEditor.Views.EditorCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Hoi4BlueprintEditor.Views"
             xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewModels"
             xmlns:converters="clr-namespace:Hoi4BlueprintEditor.Core.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vms:EditorCanvasViewModel}">

    <UserControl.Resources>
        <converters:CoordinateToPixelConverter x:Key="CoordinateToPixelConverter"/>
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding Nodes}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <views:FocusNodeView/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>

        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Left" Value="{Binding X, Converter={StaticResource CoordinateToPixelConverter}, ConverterParameter=96}"/>
                <Setter Property="Canvas.Top" Value="{Binding Y, Converter={StaticResource CoordinateToPixelConverter}, ConverterParameter=130}"/>
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
</UserControl>
