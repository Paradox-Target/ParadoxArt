<UserControl
    x:Class="Hoi4BlueprintEditor.Views.ActivateWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewsModels"
    d:DataContext="{d:DesignInstance vms:ActivateWindowViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="1000"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox
            Grid.Row="0"
            MinWidth="300"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            ui:ControlHelper.Header="激活码"
            Text="{Binding ActivationCode, Mode=TwoWay}" />
        <Button
            Grid.Row="1"
            HorizontalAlignment="Center"
            Command="{Binding ActivateCommand}"
            Content="{Binding ButtonText, Mode=TwoWay}" />
    </Grid>
</UserControl>
