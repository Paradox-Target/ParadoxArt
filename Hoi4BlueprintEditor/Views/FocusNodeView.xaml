<UserControl
    x:Class="Hoi4BlueprintEditor.Views.FocusNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewsModels"
    d:DataContext="{d:DesignInstance vms:FocusNodeViewModel}"
    d:DesignHeight="70"
    d:DesignWidth="50"
    mc:Ignorable="d">
    <Grid
        Width="100"
        Height="110"
        Background="#555"
        ToolTip="{Binding Model.Id}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image
            Grid.Row="0"
            Width="{Binding Width}"
            Height="{Binding Height}"
            Source="{Binding BitmapSource}" />

        <TextBlock
            Grid.Row="1"
            Margin="2,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="12"
            Foreground="White"
            Text="{Binding LocalizedName}"
            TextTrimming="CharacterEllipsis" />
    </Grid>
</UserControl>
