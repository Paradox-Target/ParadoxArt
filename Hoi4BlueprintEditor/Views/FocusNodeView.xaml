<UserControl
    d:DataContext="{d:DesignInstance vms:FocusNodeViewModel}"
    d:DesignHeight="70"
    d:DesignWidth="50"
    mc:Ignorable="d"
    x:Class="Hoi4BlueprintEditor.Views.FocusNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:Hoi4BlueprintEditor.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewsModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid
        Height="{x:Static helper:FocusMapMetrics.CellHeight}"
        ToolTip="{Binding Model.Id}"
        Width="{x:Static helper:FocusMapMetrics.CellWidth}">

        <!-- <Rectangle Fill="#555"> -->
        <!--     <Rectangle.RenderTransform> -->
        <!--         <TransformGroup> -->
        <!--             <ScaleTransform ScaleY="{x:Static helper:FocusMapMetrics.FocusFactor}" /> -->
        <!--             <TranslateTransform Y="{x:Static helper:FocusMapMetrics.FocusCenterOffsetVertical}" /> -->
        <!--         </TransformGroup> -->
        <!--     </Rectangle.RenderTransform> -->
        <!-- </Rectangle> -->
        <Image
            Height="{Binding Height}"
            Source="{Binding BitmapSource}"
            Width="{Binding Width}">
            <!-- <Image.RenderTransform> -->
            <!--     <TransformGroup> -->
            <!--         <TranslateTransform Y="{x:Static helper:FocusMapMetrics.FocusContentUpOffset}" /> -->
            <!--     </TransformGroup> -->
            <!-- </Image.RenderTransform> -->
        </Image>

        <TextBlock
            FontSize="12"
            Foreground="White"
            HorizontalAlignment="Center"
            Margin="2,0"
            Text="{Binding LocalizedName}"
            TextTrimming="CharacterEllipsis">
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <TranslateTransform Y="{x:Static helper:FocusMapMetrics.FocusHeight}" />
                    <TranslateTransform Y="{x:Static helper:FocusMapMetrics.FocusContentUpOffset}" />
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</UserControl>
