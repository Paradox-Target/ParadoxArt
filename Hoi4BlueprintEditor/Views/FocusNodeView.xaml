<UserControl
    x:Class="Hoi4BlueprintEditor.Views.FocusNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:Hoi4BlueprintEditor.ViewModels"
    d:DataContext="{d:DesignInstance Type=vms:FocusNodeViewModel}"
    d:DesignHeight="70"
    d:DesignWidth="50"
    mc:Ignorable="d">
    <Grid
        Width="48"
        Height="68"
        Background="#555"
        ToolTip="{Binding Id}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Margin="2"
            Background="#333"
            BorderBrush="Gray"
            BorderThickness="1">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="10"
                Foreground="White"
                Text="ICON" />
        </Border>

        <TextBlock
            Grid.Row="1"
            Margin="2,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="9"
            Foreground="White"
            Text="{Binding Id}"
            TextTrimming="CharacterEllipsis" />
    </Grid>
</UserControl>
